<h1>My tasks</h1>
<ul class="list-unstyled">
  <% @tasks.each do |task| %>
    <% if task.completed %>
      <li>☑ <%= link_to task.title, task.id %></li>
    <% else %>
      <li>☐ <%= link_to task.title, task.id %></li>
    <% end %>
  <% end %>
</ul>